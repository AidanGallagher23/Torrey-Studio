# CLAUDE.md — Torrey Studio

Aidan's copywriting + web design studio site (he is the founder). Static HTML/CSS/JS, multi-page: `index.html`, `about.html`, `start.html` (intake), 12 service pages under `services/`, shared nav in `nav.js`, brand assets in `brand/`.

## Continue where we left off
- GitHub repo (github.com/AidanGallagher23/Torrey-Studio) is the source of truth. An older single-page draft on `~/Desktop/index.html` is superseded — do not resurrect its removed "results" section (removed deliberately: fabricated proof).
- History: CTA wiring, intake page, nav component, and service pages were added June 2026.

## Rules
- No fabricated testimonials, metrics, or client results — this is a real business.
- When done working: commit and push to `main`, or push a `claude/<description>` branch if unfinished. Update this file's status first.
